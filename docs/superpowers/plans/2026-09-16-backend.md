# Utility Expense Backend Implementation Plan

> For agentic workers: use superpowers:subagent-driven-development; test each change before implementation.

**Goal:** Complete and harden the supplied Firebase-backed NestJS API for utility_expense workflows.
**Architecture:** Preserve modular NestJS controllers/services and Firebase gateways. Extend transactional workflows without changing existing collection identities.
**Tech Stack:** Node 24, NestJS 11, TypeScript, Firebase Admin, Firestore, Storage, Jest.
**Spec:** docs/superpowers/specs/2026-09-16-backend.md

## Global Constraints
Preserve /v1 contracts and integer Wh/paisa. No service-account keys in deliverables. No live database writes. Flutter UI unchanged; document remote integration truthfully. One active house and manager per house. Owner/manager may manage rooms; renter data remains isolated.

### Task 1: Align member and room workflows
Files: src/members/*, src/units/*, src/dashboard/*, test/workflow.spec.ts, test/api.spec.ts.
Interfaces: existing MembersService.assign and UnitsService.create remain compatible; optional unitId supports unassigned renters; PUT /v1/units/:id/resident assigns a previously added active renter.
- [x] Add regression tests: assigning renter without unit succeeds; unassigned renter dashboard returns unit=null; managers create units; assignment fails cross-house, for occupied room, and for already assigned renter.
- [x] Run tests and confirm expected failures.
- [x] Implement reads-before-writes transactions; synchronize profile/member/unit snapshots and audit history; editing/removing an unassigned renter works and adjusts residentCount without clearing managerUid.
- [x] Run regression and existing suites; review security and transaction correctness.

### Task 2: Operational hardening and reports
Files: src/health/*, src/config/*, src/billing/*, scripts/*, test/production.spec.ts.
- [x] Test /readyz success with a functioning Store and 503 with failed dependency without leaking error; test report role separation and max 12 months.
- [x] Add bounded Store health read, read-only Firebase diagnostics, reporting endpoint, security headers and shutdown behavior.
- [x] Verify build, types and all tests.

- [x] Add direct room/member detail endpoints with role checks and current revision, avoiding unbounded paginated scans for detail screens; add and pass 2 HTTP regressions.
- [x] Correct diagnostic to run compiled code in production images without dev build tooling.

### Task 3: Documentation and delivery
Files: README.md, .env.example, Dockerfile, .dockerignore, .github/workflows/ci.yml, docs/API.md, docs/SCHEMA.md, docs/FLUTTER_INTEGRATION.md, docs/VERIFICATION.md.
- [x] Document every route and roles, decimal input/integer output, Firebase auth setup, house bootstrap, reading preview/publish, photo and report contracts.
- [x] Document actual Flutter synchronous repository mismatch and required asynchronous mapping.
- [x] Run dependency audit, emulator integration and read-only live diagnostics if reachable; record actual outcomes.
- [x] Review all changes; package clean source and test evidence without credentials or generated dependencies.
